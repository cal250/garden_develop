import { SmoothCounter } from "../index";

const meta = {
  title: "Components/Atoms/SmoothCounter",
  component: SmoothCounter,
};

export default meta;

export const Default = {
  args: {
    value: 60,
    duration: 10000,
  },
};
